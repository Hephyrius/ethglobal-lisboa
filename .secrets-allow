# Known-public high-entropy strings, for scripts/check-secrets.sh.
#
# One extended-regex pattern per line, matched against the candidate token. Blank lines and
# lines starting with # are ignored. Suppressions are COUNTED and reported, so silencing
# something here is visible rather than invisible.
#
# ⚠️ THE ONE RULE: only genuinely public identifiers belong in this file. If you ever paste a
# credential here to quiet the checker, you have committed the credential — the file is tracked,
# and you will have done by hand exactly what `git add -A` did to env.txt in 408072f.
#
# Every entry needs a reason. "It was noisy" is not one.

# The Graph subgraph deployment IDs. Public by construction: they are content hashes of published
# subgraphs, they appear in gateway URLs, and data/GRAPH-USAGE.md quotes them deliberately because
# The Graph's own submission criteria ask which subgraphs we used. Base58, ~4.8 bits/char, which is
# why the entropy rule finds them and why the entropy rule is right to.
#
# Copied from the files that carry them, not retyped: two of these were wrong the first time
# because I reconstructed them from a redacted snippet, and a near-miss pattern silently allowlists
# nothing while looking like it works.
^33ex1ExmYQtwGVwri1AP3oMFPGSce6YbocBP7fWbsBrg$
^GQFbb95cE6d8mV989mL5figjaGaKCQB3xqYrr1bRyXqF$
^FUbEPQw1oMghy39fwWBFY5fE6MXPXZQtjncQy2cXdrNS$
^DZz4kDTdmzWLWsV373w2bSmoar3umKKH9y82SUKr5qmp$
